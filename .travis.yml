language: python

services:
  - docker

before_install:
  - docker build -t chai/better-adversarial-defenses .
  - docker ps -a

script:
  - docker run -it chai/better-adversarial-defenses /bin/bash -c "./test.sh --no_mujoco_license"
